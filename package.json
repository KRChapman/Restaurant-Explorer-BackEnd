{
  "name": "example-create-react-app-express",
  "version": "1.0.0",
  "scripts": {
    "client": "npm start",
    "test": "jest --watch --runInBand --setupFiles dotenv/config",
    "server": "nodemon server/index.js",
    "debug": "cd server && nodemon --inspect-brk index.js"
  },
  "dependencies": {
    "body-parser": "^1.18.3",
    "cors": "^2.8.5",
    "express": "^4.16.2",
    "node-fetch": "^2.6.0"
  },
  "devDependencies": {
    "concurrently": "^3.6.1",
    "dotenv": "^8.0.0",
    "env-cmd": "^9.0.1",
    "jest": "23.6.0",
    "supertest": "^4.0.2"
  }
}
